# Optional WGS84 projection; local-metre imports use Python standard library only.
pyproj==3.7.2
# Optional OSM PBF/XML snapshot reader; retained license belongs to pyosmium.
osmium==4.3.1

# Optional local Copernicus COG decoding; no GDAL network reads.
rasterio==1.4.4

# Optional bounded OSM geometry intersection; data remains ODbL.
shapely==2.1.2
